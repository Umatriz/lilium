def add :: (int a, int b) -> int {
    a + b
}

def main :: {
    double = x |-> x * 2;

    a = 30;
    b = double(16);
    c = add(a, b);

    println("Hello! ", a, " plus ", b, " equals to ", c);
}
